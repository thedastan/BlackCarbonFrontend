import { createSlice } from '@reduxjs/toolkit';

const initialState = {
    
}

const main = createSlice({
    name: 'main',
    initialState,
    reducers: {
        
    },
})

export const {
    
} = main.actions
export default main.reducer;